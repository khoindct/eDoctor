import api from "../../api";
import { Dispatch } from "redux";
import { Action } from "../actions";

const axios = api();

export const getClinics = () => async (dispatch: Dispatch<Action>) => {};

export const createClinic = () => async (dispatch: Dispatch<Action>) => {};
